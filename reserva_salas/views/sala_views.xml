<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--  VISTAS EXISTENTES -->
    <record id="view_sala_tree" model="ir.ui.view">
        <field name="name">reserva.sala.tree</field>
        <field name="model">reserva.sala</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="capacidad"/>
                <field name="equipamiento"/>
            </tree>
        </field>
    </record>

    <!-- Vista formulario 2.0 -->
    <record id="view_sala_form" model="ir.ui.view">
        <field name="name">reserva.sala.form</field>
        <field name="model">reserva.sala</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <!-- Grupo 1: Información básica -->
                    <group string="Información de la Sala" col="2">
                        <field name="name" 
                               string="Nombre de la Sala"
                               placeholder="Ej: Sala de Conferencias A..."
                               help="Nombre identificativo de la sala"/>
                        
                        <field name="capacidad" 
                               string="Capacidad máxima"
                               placeholder="Ej: 10, 20, 50..."
                               help="Número máximo de personas que caben en la sala"/>
                    </group>
                    
                    <!-- Grupo 2: Equipamiento -->
                    <group string="Equipamiento Disponible">
                        <field name="equipamiento" 
                               placeholder="Ej: Proyector, pizarra blanca, wifi, aire acondicionado..."
                               widget="textarea"
                               help="Equipamiento y comodidades"/>
                    </group>
                </sheet>

                <!-- Reservas existentes (solo para salas existentes) -->
                <field name="reserva_ids" 
                       mode="tree,form"
                       string="Reservas de esta sala"
                       invisible="not id"/>
            </form>
        </field>
    </record>

    <!-- ACCIONES -->
    <record id="action_sala" model="ir.actions.act_window">
        <field name="name">Salas</field>
        <field name="res_model">reserva.sala</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" ref="view_sala_tree"/>
    </record>
</odoo>